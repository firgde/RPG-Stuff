# 4つの中から絞り込み
    execute if score #element buffer matches 1 run tag @s add arrow.fire
    execute if score #element buffer matches 2 run tag @s add arrow.ice
    execute if score #element buffer matches 3 run tag @s add arrow.wind
    execute if score #element buffer matches 4 run tag @s add arrow.thunder
